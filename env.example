# Local Auth System Configuration
# This file is for non-sensitive configuration only
# All passwords are securely configured through the web interface

# PostgreSQL Database
PG_USER=authentik
PG_DB=authentik

# LDAP Configuration
LDAP_ORGANISATION=Local Auth
LDAP_DOMAIN=local.auth
LDAP_BASE_DN=dc=local,dc=auth

# RADIUS Configuration
# Add RADIUS clients after initial setup
# Format: CLIENT_NAME:CLIENT_IP:CLIENT_SECRET;CLIENT_NAME2:CLIENT_IP2:CLIENT_SECRET2
# Example: switch1:192.168.1.10:switch-secret;router1:192.168.1.1:router-secret
RADIUS_CLIENTS=

# Logging
AUTHENTIK_LOG_LEVEL=info

# Optional: External URLs (if not using default ports)
# AUTHENTIK_HOST=authentik.local.auth
# AUTHENTIK_PORT=9000

# ===========================================================
# IMPORTANT: Security Notice
# ===========================================================
# Passwords are not stored in this file.
#
# Initial Setup:
#   1. Run: docker-compose up -d
#   2. Open: http://localhost:8000
#   3. Complete the secure web-based setup
#
# Password Management:
#   - Reset admin password: ./scripts/reset-admin-password.sh
#   - Reset LDAP password: ./scripts/reset-ldap-password.sh
#   - Reset database password: ./scripts/reset-database-password.sh
#
# All passwords are stored using Argon2 hashing in config/system_config.json
# ===========================================================
